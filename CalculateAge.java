
import java.time.*;
import java.util.*;
import java.util.Locale;

public class CalculateAge {
    private static LocalDate LocalDatedob;


        public static void main (String[]args){
        //Obtain the local instance date
//        LocalDate dob = LocalDate.of(1999, 12, 9);
        //Obtain the current date from calender
        LocalDate curDate = LocalDate.now();
        //Calculate the Diffrence between two dates
//        Period period = Period.between(dob, curDate);
        System.out.printf("today date is:" + LocalDatedob);
    }
    }
